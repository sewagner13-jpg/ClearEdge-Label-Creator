# Development & testing dependencies — not required for production deployment
# Install with: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
pytest-mock==3.12.0
