# What's New

- Label extraction now uses OpenAI.
- The browser upload flow is the primary workflow.
- The backend runs on Railway and the static frontend runs on Netlify.
- Generated label PDFs are stored by the app runtime and exposed through `/api/v1/labels/{label_id}/download`.
